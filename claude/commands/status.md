Check the current project status:
1. Run `git status -sb` to see uncommitted changes
2. Run `git log --oneline -5` to see recent commits
3. Check if any tests exist and run them
4. Give a brief summary of: current branch, uncommitted files, last 3 commits, and any failing tests
